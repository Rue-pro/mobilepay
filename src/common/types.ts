export type Operators = {
  id: number;
  name: string;
  logoURL: string;
};

export type OperatorPayData = {
  id: number;
  phone: string;
  money: number;
};

export type ResponseResult = {
  code: number;
  message: string;
};
